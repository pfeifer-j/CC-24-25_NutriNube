<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match app.**>
  @type stdout
  <format>
    @type json
  </format>
</match>

<match httpd.access>
  @type stdout
  <format>
    @type json
  </format>
</match>
